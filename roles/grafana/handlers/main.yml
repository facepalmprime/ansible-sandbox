---
- name: Restart grafana
  ansible.builtin.systemd:
    name: grafana
    state: restarted
    daemon_reload: true

- name: Update CA trust
  ansible.builtin.command:
    cmd: "{{ 'update-ca-trust' if ansible_os_family == 'RedHat' else 'update-ca-certificates' }}"
  changed_when: true
