---
- name: Prepare MariaDB molecule container
  hosts: all
  gather_facts: false
  become: true

  tasks:
    - name: Install python3-PyMySQL for community.mysql modules
      ansible.builtin.dnf:
        name: python3-PyMySQL
        state: present
