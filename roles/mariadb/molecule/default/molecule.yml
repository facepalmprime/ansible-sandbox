---
dependency:
  name: galaxy
driver:
  name: podman
platforms:
  - name: rocky8-mariadb-test
    image: docker.io/geerlingguy/docker-rockylinux8-ansible:latest
    pre_build_image: true
    privileged: true
    command: /usr/sbin/init
provisioner:
  name: ansible
  env:
    ANSIBLE_CONFIG: "../../ansible.cfg"
  inventory:
    group_vars:
      all:
        ansible_python_interpreter: /usr/bin/python3
        env_name: molecule
        vault_gitea_db_password: "molecule_test_db_pass"
        vault_mariadb_root_password: "molecule_test_root_pass"
verifier:
  name: ansible
